Работа над ошибками, что изменил:

- файл mart.f_sales.sql; про комментарий к замене left join на inner join; 
логика была следующей:
Я предположил сценарий при котором в staging попадают даты, которые отсутствуют в календаре: кривые строки с пропущенными датами или с неверным форматом. Согласен, что перемудрил так как в БД в staging таблице для поля date_time задан тип данных timestamp c not null constraint. Поэтому при попытке вставить такие значения запрос и DAG ляжет с ошибкой.
Исправил на первоначальный вариант с left join и удалил комментарий.

- файл alter_schema.sql; добавил DDL на создание таблицы для витрины;

- файл mart.f_customer_retention_create.sql;
переименовал в mart.f_customer_retention_update.sql, изменил логику на инкрементальную загрузку недельных данных

- файл mart.f_customer_retention_delete_current.sql;
добавил скрипт для удаления данных за текущий период

- файл mart.f_customer_retention_refresh.sql; удалил так как материализованное представление заменил на таблицу в которую загружаем данные инкрементом

- файл load_mart.py
для словаря ARGS, ключа retries установил количество повторов на 2
в функции upload_data_to_staging, для pd.read_csv задал index_col = 0, 
для DAG задал параметр max_active_runs = 1, p.s. отличный совет, долго искал как задать такое поведение DAG +++

задал загрузку dimensions в цикле, отличнный совет!